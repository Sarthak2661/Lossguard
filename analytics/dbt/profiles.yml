lossguard:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'lossguard') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'lossguard_local_only') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'lossguard') }}"
      schema: analytics
      threads: 4

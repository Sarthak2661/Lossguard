apiVersion: 1

providers:
  - name: LossGuard
    orgId: 1
    folder: LossGuard
    folderUid: lossguard
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
